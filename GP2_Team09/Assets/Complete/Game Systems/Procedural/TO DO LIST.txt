===============================================================
// TO DO LIST
===============================================================

# Store the button events in data and recreate them
  https://docs.unity3d.com/ScriptReference/Events.UnityEventBase.GetPersistentListenerState.html
  
  Instead of storing the persistent target object itself,
  store the that persistent target's GUID so I can easily find it
  in the dictionary 
  Also get the persistent target object's script type